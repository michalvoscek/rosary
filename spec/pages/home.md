# Page: Home (`/`)

## Purpose

The landing page. Shows all weekdays but highlights mystery for current weekday.

## Route

`/`

## Sections Detail

### All weekdays

- 7 cards ordered Monday → Sunday
- The current weekday has `isRecommended` set to `true`
- Each is a `<MysteryCard>`
